# 07 - 工具函数抽离

## 🎯 目标

提取重复的工具函数，建立统一的工具库，提升代码复用性。

## 📊 当前问题

### 发现的重复逻辑

1. **Slug 生成**：在多个表单中重复实现
2. **阅读时间计算**：在文章创建时重复计算
3. **日期格式化**：使用不一致的格式化方式
4. **错误处理**：错误处理逻辑分散

---

## 🔧 解决方案

### 工具库设计

```
lib/utils/
├── slug.ts           # Slug 生成
├── reading-time.ts   # 阅读时间计算
├── date.ts           # 日期格式化
├── validation.ts     # 通用验证
├── error.ts          # 错误处理
└── index.ts          # 统一导出
```

---

## 📝 实施步骤

### 步骤 1：创建 Slug 工具

```typescript
// lib/utils/slug.ts - 新建

/**
 * 将文本转换为 URL 友好的 slug
 * @param text 原始文本
 * @param maxLength 最大长度（默认 50）
 * @returns slug 字符串
 */
export function slugify(text: string, maxLength: number = 50): string {
  return text
    .toLowerCase()
    .trim()
    // 移除特殊字符
    .replace(/[^a-z0-9\s-]/g, '')
    // 将空格替换为连字符
    .replace(/\s+/g, '-')
    // 移除多余的连字符
    .replace(/-+/g, '-')
    // 移除首尾连字符
    .replace(/^-+|-+$/g, '')
    // 限制长度
    .slice(0, maxLength)
}

/**
 * 生成唯一的 slug（添加时间戳）
 */
export function generateUniqueSlug(text: string): string {
  const baseSlug = slugify(text, 40)
  const timestamp = Date.now().toString(36)
  return `${baseSlug}-${timestamp}`
}

/**
 * 验证 slug 格式
 */
export function isValidSlug(slug: string): boolean {
  return /^[a-z0-9-]+$/.test(slug)
}
```

---

### 步骤 2：创建阅读时间计算工具

```typescript
// lib/utils/reading-time.ts - 新建

/**
 * 计算文章阅读时间
 * @param content Markdown 内容
 * @param wordsPerMinute 每分钟阅读字数（默认 200）
 * @returns 阅读时间（分钟）
 */
export function calculateReadingTime(
  content: string,
  wordsPerMinute: number = 200
): number {
  // 移除 Markdown 语法
  const plainText = content
    .replace(/```[\s\S]*?```/g, '') // 移除代码块
    .replace(/`[^`]+`/g, '') // 移除行内代码
    .replace(/!\[.*?\]\(.*?\)/g, '') // 移除图片
    .replace(/\[([^\]]+)\]\([^\)]+\)/g, '$1') // 移除链接，保留文本
    .replace(/[#*_~`]/g, '') // 移除 Markdown 标记

  // 计算字数
  const wordCount = plainText.split(/\s+/).filter(Boolean).length

  // 计算阅读时间（至少 1 分钟）
  return Math.max(1, Math.ceil(wordCount / wordsPerMinute))
}

/**
 * 格式化阅读时间
 */
export function formatReadingTime(minutes: number): string {
  if (minutes < 1) return '少于 1 分钟'
  if (minutes === 1) return '1 分钟'
  return `${minutes} 分钟`
}
```

---

### 步骤 3：创建日期格式化工具

```typescript
// lib/utils/date.ts - 新建

/**
 * 格式化日期
 * @param date 日期对象或字符串
 * @param locale 语言环境（默认 zh-CN）
 * @returns 格式化后的日期字符串
 */
export function formatDate(date: Date | string, locale: string = 'zh-CN'): string {
  return new Date(date).toLocaleDateString(locale, {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  })
}

/**
 * 格式化日期时间
 */
export function formatDateTime(date: Date | string, locale: string = 'zh-CN'): string {
  return new Date(date).toLocaleString(locale, {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    hour: '2-digit',
    minute: '2-digit',
  })
}

/**
 * 格式化相对时间（如：3 分钟前）
 */
export function formatRelativeTime(date: Date | string): string {
  const now = new Date()
  const target = new Date(date)
  const diffInSeconds = Math.floor((now.getTime() - target.getTime()) / 1000)

  if (diffInSeconds < 60) return '刚刚'
  if (diffInSeconds < 3600) return `${Math.floor(diffInSeconds / 60)} 分钟前`
  if (diffInSeconds < 86400) return `${Math.floor(diffInSeconds / 3600)} 小时前`
  if (diffInSeconds < 604800) return `${Math.floor(diffInSeconds / 86400)} 天前`
  if (diffInSeconds < 2592000) return `${Math.floor(diffInSeconds / 604800)} 周前`
  if (diffInSeconds < 31536000) return `${Math.floor(diffInSeconds / 2592000)} 个月前`
  return `${Math.floor(diffInSeconds / 31536000)} 年前`
}

/**
 * 格式化为 ISO 字符串
 */
export function toISOString(date: Date | string): string {
  return new Date(date).toISOString()
}

/**
 * 判断是否为今天
 */
export function isToday(date: Date | string): boolean {
  const today = new Date()
  const target = new Date(date)
  return (
    today.getFullYear() === target.getFullYear() &&
    today.getMonth() === target.getMonth() &&
    today.getDate() === target.getDate()
  )
}
```

---

### 步骤 4：创建验证工具

```typescript
// lib/utils/validation.ts - 新建

/**
 * 验证邮箱格式
 */
export function isValidEmail(email: string): boolean {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return emailRegex.test(email)
}

/**
 * 验证 URL 格式
 */
export function isValidUrl(url: string): boolean {
  try {
    new URL(url)
    return true
  } catch {
    return false
  }
}

/**
 * 验证用户名格式
 * 规则：3-20 个字符，只能包含字母、数字、下划线和连字符
 */
export function isValidUsername(username: string): boolean {
  const usernameRegex = /^[a-zA-Z0-9_-]{3,20}$/
  return usernameRegex.test(username)
}

/**
 * 验证密码强度
 * 规则：至少 6 个字符
 */
export function isValidPassword(password: string): boolean {
  return password.length >= 6
}

/**
 * 验证密码强度（严格）
 * 规则：至少 8 个字符，包含大小写字母、数字和特殊字符
 */
export function isStrongPassword(password: string): boolean {
  const strongPasswordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/
  return strongPasswordRegex.test(password)
}

/**
 * 清理 HTML（防止 XSS）
 * 注意：这是简单实现，生产环境建议使用 DOMPurify
 */
export function sanitizeHtml(html: string): string {
  return html
    .replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, '')
    .replace(/<iframe\b[^<]*(?:(?!<\/iframe>)<[^<]*)*<\/iframe>/gi, '')
    .replace(/on\w+="[^"]*"/gi, '')
    .replace(/on\w+='[^']*'/gi, '')
}

/**
 * 截断文本
 */
export function truncate(text: string, maxLength: number, suffix: string = '...'): string {
  if (text.length <= maxLength) return text
  return text.slice(0, maxLength - suffix.length) + suffix
}
```

---

### 步骤 5：创建错误处理工具

```typescript
// lib/utils/error.ts - 新建

/**
 * 应用错误基类
 */
export class AppError extends Error {
  constructor(
    message: string,
    public code: string,
    public statusCode: number = 500,
    public details?: any
  ) {
    super(message)
    this.name = 'AppError'
    Error.captureStackTrace(this, this.constructor)
  }
}

/**
 * 404 错误
 */
export class NotFoundError extends AppError {
  constructor(message: string = '资源未找到', details?: any) {
    super(message, 'NOT_FOUND', 404, details)
    this.name = 'NotFoundError'
  }
}

/**
 * 401 错误
 */
export class UnauthorizedError extends AppError {
  constructor(message: string = '未授权，请先登录', details?: any) {
    super(message, 'UNAUTHORIZED', 401, details)
    this.name = 'UnauthorizedError'
  }
}

/**
 * 403 错误
 */
export class ForbiddenError extends AppError {
  constructor(message: string = '权限不足', details?: any) {
    super(message, 'FORBIDDEN', 403, details)
    this.name = 'ForbiddenError'
  }
}

/**
 * 400 错误
 */
export class ValidationError extends AppError {
  constructor(message: string = '验证失败', details?: any) {
    super(message, 'VALIDATION_ERROR', 400, details)
    this.name = 'ValidationError'
  }
}

/**
 * 409 错误
 */
export class ConflictError extends AppError {
  constructor(message: string = '资源冲突', details?: any) {
    super(message, 'CONFLICT', 409, details)
    this.name = 'ConflictError'
  }
}

/**
 * 统一错误处理
 */
export function handleError(error: unknown): string {
  if (error instanceof AppError) {
    return error.message
  }

  if (error instanceof Error) {
    return error.message
  }

  return '未知错误，请稍后重试'
}

/**
 * 判断是否为特定类型的错误
 */
export function isNotFoundError(error: unknown): error is NotFoundError {
  return error instanceof NotFoundError
}

export function isUnauthorizedError(error: unknown): error is UnauthorizedError {
  return error instanceof UnauthorizedError
}

export function isValidationError(error: unknown): error is ValidationError {
  return error instanceof ValidationError
}
```

---

### 步骤 6：统一导出

```typescript
// lib/utils/index.ts - 修改

// 现有的 cn 工具保持不变
export { cn } from './cn'

// 新增工具函数导出
export * from './slug'
export * from './reading-time'
export * from './date'
export * from './validation'
export * from './error'
```

---

## 📋 使用示例

### 示例 1：在表单中使用 Slug 工具

```typescript
// 改造前
const generateSlug = () => {
  const title = form.getValues('title')
  const slug = title
    .toLowerCase()
    .replace(/[^a-z0-9\s-]/g, '')
    .replace(/\s+/g, '-')
    .slice(0, 50)
  form.setValue('slug', slug)
}

// 改造后
import { slugify } from '@/lib/utils'

const generateSlug = () => {
  const title = form.getValues('title')
  form.setValue('slug', slugify(title))
}
```

---

### 示例 2：在文章创建时计算阅读时间

```typescript
// 改造前
const wordCount = content.split(/\s+/).length
const readingTime = Math.ceil(wordCount / 200)

// 改造后
import { calculateReadingTime } from '@/lib/utils'

const readingTime = calculateReadingTime(content)
```

---

### 示例 3：格式化日期

```typescript
// 改造前
{new Date(post.createdAt).toLocaleDateString('zh-CN')}

// 改造后
import { formatDate, formatRelativeTime } from '@/lib/utils'

{formatDate(post.createdAt)}
{formatRelativeTime(post.createdAt)}  // "3 小时前"
```

---

### 示例 4：错误处理

```typescript
// 改造前
try {
  await createPost(data)
} catch (error) {
  if (error instanceof Error && error.message.includes('Unauthorized')) {
    toast.error('请先登录')
  } else {
    toast.error('操作失败')
  }
}

// 改造后
import { handleError, isUnauthorizedError } from '@/lib/utils'

try {
  await createPost(data)
} catch (error) {
  if (isUnauthorizedError(error)) {
    toast.error('请先登录')
  } else {
    toast.error(handleError(error))
  }
}
```

---

## 📊 预期效果

### 代码量变化

| 指标 | 改造前 | 改造后 | 提升 |
|------|--------|--------|------|
| 重复工具函数 | 15+ 处 | 0 处 | 100% ↓ |
| 工具函数代码 | 分散 | 集中 200 行 | - |
| 错误处理一致性 | 低 | 高 | - |

### 维护性提升

**场景：修改日期格式**

**改造前**：
```bash
# 需要修改 10+ 个文件
find . -name "*.tsx" -exec sed -i 's/toLocaleDateString/新格式/g' {} +
```

**改造后**：
```typescript
// 只需修改 1 处
// lib/utils/date.ts
export function formatDate(date: Date | string) {
  return new Date(date).toLocaleDateString('zh-CN', {
    // 修改这里即可全局生效
  })
}
```

---

## ✅ 验收标准

1. ✅ 所有工具函数集中在 `lib/utils/` 目录
2. ✅ 每个工具函数有清晰的 JSDoc 注释
3. ✅ 所有重复逻辑已替换为工具函数调用
4. ✅ 错误处理统一使用自定义错误类
5. ✅ 工具函数有单元测试（可选）

---

## 🔄 迁移步骤

### 第一步：创建工具函数（30 分钟）
1. 创建 `lib/utils/` 目录下的所有文件
2. 实现所有工具函数
3. 更新 `lib/utils/index.ts` 导出

### 第二步：替换现有代码（30 分钟）
1. 搜索重复的 slug 生成逻辑，替换为 `slugify()`
2. 搜索日期格式化，替换为 `formatDate()` 或 `formatRelativeTime()`
3. 搜索阅读时间计算，替换为 `calculateReadingTime()`
4. 搜索错误处理，替换为统一的错误类

### 第三步：测试验证（15 分钟）
1. 测试 slug 生成功能
2. 测试日期格式化显示
3. 测试错误处理流程
4. 测试阅读时间计算

**总耗时**：约 1 小时

---

## 📝 注意事项

1. **保持向后兼容**：迁移过程中确保不破坏现有功能
2. **渐进式迁移**：可以先创建工具函数，再逐步替换
3. **测试覆盖**：关键工具函数建议添加单元测试
4. **文档完善**：每个工具函数都应有清晰的注释

---

**下一步**：[08 - 设计令牌系统](./08-设计令牌系统.md)
